splitw -h -c "#{pane_current_path}"
resize-pane -x "35%"

select-pane -t 1
splitw -v -c "#{pane_current_path}"

resize-pane -y "30%"

select-pane -t 3
splitw -v -c "#{pane_current_path}"
resize-pane -y "30%"

select-pane -t 1
